// Central animation configuration for fade-in components
const animationConfig = {
  // vertical offset to animate from (can be any CSS length)
  y: '24px',
  // duration of the animation
  duration: '420ms',
  // easing function
  easing: 'cubic-bezier(.2,.9,.3,1)',
  // starting scale value for the element (animates to 1)
  scale: '0.98'
  ,
  // default delay before the transition starts
  delay: '0ms'
}

export default animationConfig

